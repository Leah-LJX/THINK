import org.jfree.chart.annotations.DialTextAnnotation;

public class DialTextAnnotationExample {

    public static void main(String[] args) {
        DialTextAnnotation annotation = new DialTextAnnotation();
        String label = "New Label";
        
        annotation.setLabel(label);
    }
}