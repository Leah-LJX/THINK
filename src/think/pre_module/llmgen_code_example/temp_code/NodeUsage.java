import some.package.Node;

public class NodeUsage {
    public static void main(String[] args) {
        Node node = new Node();
        String value = "some value";
        node.setNodeValue(value);
    }
}